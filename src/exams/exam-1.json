[
  {
    "number": 1,
    "question": "A support application must classify screenshots where visual layout affects the result. The selected approach must produce clear request, response, and operational behavior across the workflow. Which Messages API design best preserves the needed evidence?",
    "options": [
      "Run OCR and discard the image before sending extracted text, even when layout determines the correct classification. Reviewers would evaluate whether this can preserve image evidence throughout classification.",
      "Send screenshot as an image content block with the ticket question in the same message. The design would use this reasoning to preserve image evidence throughout classification.",
      "Upload screenshots as unrelated files, then ask Claude to infer which file belongs to each ticket. Reviewers would evaluate whether this can preserve image evidence throughout classification.",
      "Describe each screenshot from memory and omit the image whenever the application calls Claude. The selected method would be used to preserve image evidence throughout classification."
    ],
    "correctAnswers": [
      "Send screenshot as an image content block with the ticket question in the same message. The design would use this reasoning to preserve image evidence throughout classification."
    ],
    "explanation": "An image content block preserves the pixels and layout that the classifier needs. Placing the ticket question in the same user message also makes the requested analysis explicit. That directly supports the need to preserve image evidence throughout classification. The Messages API accepts image blocks from supported sources alongside text blocks. This request shape keeps the screenshot and its task together without a lossy preprocessing step. It gives reviewers concrete evidence instead of relying on an informal assumption. Evaluation cases should include tickets whose text is identical but whose layouts imply different categories. Passing those cases would confirm that the application is using visual evidence rather than OCR alone. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 2,
    "question": "A regulated extraction service requires reproducible validation and controlled model upgrades. The selected approach must produce clear request, response, and operational behavior across the workflow. Which TWO practices best support that requirement? Rotate among model aliases whenever capacity changes, allowing request-time routing to replace the version validated for regulated production use. Reviewers would evaluate whether this can pin model versions and preserve release evidence. Select a different model for each request according to temporary availability rather than validated behavior. Reviewers would evaluate whether this can pin model versions and preserve release evidence.",
    "options": [
      "Rotate among model aliases whenever capacity changes, allowing request-time routing to replace the version validated for regulated production use. Reviewers would evaluate whether this can pin model versions and preserve release evidence.",
      "Select a different model for each request according to temporary availability rather than validated behavior. Reviewers would evaluate whether this can pin model versions and preserve release evidence.",
      "Evaluate the candidate model version before deliberately changing the pinned production configuration for release. The design would use this reasoning to pin model versions and preserve release evidence.",
      "Use a moving model alias so production upgrades occur automatically without review, evaluation, or release approval. The application would follow this approach to pin model versions and preserve release evidence.",
      "Record the exact model identifier with each evaluation result and production release artifact. Reviewers would evaluate whether this can pin model versions and preserve release evidence."
    ],
    "correctAnswers": [
      "Evaluate the candidate model version before deliberately changing the pinned production configuration for release. The design would use this reasoning to pin model versions and preserve release evidence.",
      "Record the exact model identifier with each evaluation result and production release artifact. Reviewers would evaluate whether this can pin model versions and preserve release evidence."
    ],
    "explanation": "Testing the candidate against representative cases reveals behavioral changes before production configuration moves. Deliberate pin updates keep the release decision explicit and reviewable. That directly supports the need to pin model versions and preserve release evidence. The evaluation should use the same success criteria applied to the current production model. Only an accepted result should authorize the configuration change. It gives reviewers concrete evidence instead of relying on an informal assumption. Migration tests should cover task accuracy, edge cases, response handling, and any relevant performance requirements. The release plan must also identify the previously approved version for recovery. This also reduces ambiguity when the team investigates a regression. Recording the exact model identifier links each test result to the snapshot that produced it. The production release artifact then proves which evaluated model was deployed. This is the mechanism the team needs to pin model versions and preserve release evidence. That trace remains valuable when models are deprecated and a migration becomes necessary. Auditors can distinguish evidence for the old release from evidence for its replacement. The team can verify the behavior with explicit checks before release. Runtime logs should capture the returned model identifier where available and compare it with approved configuration. A mismatch can then be investigated before it contaminates validation records. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 3,
    "question": "A team needs Claude to find current public sources with citations and check order eligibility against a proprietary database with company rules. The implementation must respect the documented boundary between Claude, the tool, and the host application. Which TWO implementation choices correctly match these capabilities?",
    "options": [
      "A built-in tool can be redefined with the company's proprietary database schema and executed directly inside every client application. This would become the basis used to combine cited web search with private tools.",
      "Use a custom client tool for proprietary order eligibility checks that the application executes locally. The application would follow this approach to combine cited web search with private tools.",
      "Use the built-in web search tool for current public sources and its server-side citation workflow during research. The application would follow this approach to combine cited web search with private tools during release review.",
      "Both capabilities must use Skills because tools cannot access live data or invoke application code securely. The application would follow this approach to combine cited web search with private tools.",
      "A custom client tool automatically receives Anthropic-managed web search results and citations without application execution or result handling. The application would follow this approach to combine cited web search with private tools."
    ],
    "correctAnswers": [
      "Use the built-in web search tool for current public sources and its server-side citation workflow during research. The application would follow this approach to combine cited web search with private tools during release review.",
      "Use a custom client tool for proprietary order eligibility checks that the application executes locally. The application would follow this approach to combine cited web search with private tools."
    ],
    "explanation": "The built-in web search tool gives Claude access to current public web content. Anthropic runs this server tool rather than asking the application to implement the search backend. In this scenario, that is what allows the team to combine cited web search with private tools. Web search responses include citations to the retrieved sources. That behavior directly satisfies the public research and citation requirement. This makes the expected behavior easier to measure under production-like conditions. Using the built-in capability avoids inventing an application-specific search contract for a general web task. The team should still configure allowed domains and usage controls when its policy requires them. That makes the design easier to audit and maintain over time. A custom client tool lets the application define a schema for the proprietary eligibility inputs. Claude can request the operation without receiving direct database access. The option therefore enables the implementation to combine cited web search with private tools. The application executes the tool against its private data and company rules. It then returns only the result needed for the conversation. The resulting control can be exercised repeatedly during regression testing. Client-side execution also lets the application enforce authentication, validation, and logging at its own boundary. Those controls remain necessary even when Claude constructs a schema-valid request. Future changes can be reviewed without losing the original reasoning."
  },
  {
    "number": 4,
    "question": "A CI job needs one non-interactive Claude Code run that emits machine-readable output and exits after the response. Any command or repository instruction must work without an unnecessary manual step. Which invocation best fits? claude -p \"Summarize the repository\" --output-format json",
    "options": [
      "Run claude with -p and --output-format json so it prints one non-interactive result and exits. The team would then run one non-interactive machine-readable CI command based on that assumption before release.",
      "Pipe the prompt into /memory because that command runs CI and exits automatically. The team would apply the same decision when it must run one non-interactive machine-readable CI command.",
      "Start claude interactively and wait for a user to issue /export after completion. This would become the basis used to run one non-interactive machine-readable CI command.",
      "Run claude without -p because interactive mode automatically emits JSON for every completed turn. The selected method would be used to run one non-interactive machine-readable CI command."
    ],
    "correctAnswers": [
      "Run claude with -p and --output-format json so it prints one non-interactive result and exits. The team would then run one non-interactive machine-readable CI command based on that assumption before release."
    ],
    "explanation": "The -p or --print flag runs Claude Code non-interactively for a single prompt. It writes the response and exits instead of opening the normal interactive session. The important point is that the design can run one non-interactive machine-readable CI command. The --output-format json flag selects structured JSON output for downstream processing. That makes the result easier for the CI job to validate and consume. The implementation can record the result and compare it with the acceptance criteria. This invocation combines both requirements in one command. The job needs no user interaction after it starts. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 5,
    "question": "After an organization splits resources across workspaces, a service still completes ordinary Messages API calls with its existing key. Requests for a file created in another workspace now return HTTP 403 permission_error. What is the most likely diagnosis? The request is malformed, so the API reports invalid_request_error before checking any credential or resource access. The application would follow this approach to diagnose workspace-scoped permission failures correctly. Anthropic is temporarily overloaded, so the client should interpret every 403 response as retryable 529 condition. The selected method would be used to diagnose workspace-scoped permission failures correctly.",
    "options": [
      "The request is malformed, so the API reports invalid_request_error before checking any credential or resource access. The application would follow this approach to diagnose workspace-scoped permission failures correctly.",
      "Anthropic is temporarily overloaded, so the client should interpret every 403 response as retryable 529 condition. The selected method would be used to diagnose workspace-scoped permission failures correctly.",
      "The key authenticated successfully but lacks permission for the requested resource, so access configuration is the primary fault. The team would apply the same decision when it must diagnose workspace-scoped permission failures correctly.",
      "The organization exceeded its rate limit, so immediate retries will convert permission_error into a successful response. The selected method would be used to diagnose workspace-scoped permission failures correctly."
    ],
    "correctAnswers": [
      "The key authenticated successfully but lacks permission for the requested resource, so access configuration is the primary fault. The team would apply the same decision when it must diagnose workspace-scoped permission failures correctly."
    ],
    "explanation": "HTTP 403 permission_error indicates that the authenticated key is not allowed to use the requested resource. The continued success of other API calls shows that the key is accepted for at least some operations. This is the mechanism the team needs to diagnose workspace-scoped permission failures correctly. Workspace API keys are scoped to resources in their own workspace. Moving or creating the file elsewhere can therefore make only that resource inaccessible to the existing key. The team can verify the behavior with explicit checks before release. The team should inspect workspace ownership and the key's allowed resource scope before changing request logic. A correctly scoped key or resource placement addresses the evidence directly. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 6,
    "question": "Two prompt revisions improve different sample cases, and reviewers disagree about which to deploy. The team wants a prompt structure that remains clear as the context grows or changes over time. What is the strongest next step? Select the revision preferred by the most senior reviewer and remove the competing test cases. This would become the basis used to compare prompt revisions using held-out evidence. Choose the longer prompt because additional instructions always improve model accuracy on unseen inputs. This would become the basis used to compare prompt revisions using held-out evidence.",
    "options": [
      "Select the revision preferred by the most senior reviewer and remove the competing test cases. This would become the basis used to compare prompt revisions using held-out evidence.",
      "Choose the longer prompt because additional instructions always improve model accuracy on unseen inputs. This would become the basis used to compare prompt revisions using held-out evidence.",
      "Evaluate both prompt versions on representative held-out cases using predefined quality and latency acceptance criteria before deployment. The design would use this reasoning to compare prompt revisions using held-out evidence.",
      "Deploy both revisions randomly without defining a success metric or recording their prompt versions. The team would then compare prompt revisions using held-out evidence based on that assumption."
    ],
    "correctAnswers": [
      "Evaluate both prompt versions on representative held-out cases using predefined quality and latency acceptance criteria before deployment. The design would use this reasoning to compare prompt revisions using held-out evidence."
    ],
    "explanation": "Predefined criteria turn a subjective disagreement into a measurable comparison. Held-out cases reduce the chance that either revision is selected only because it overfits familiar examples. The important point is that the design can compare prompt revisions using held-out evidence. Quality criteria should reflect the application's actual failure costs. Latency and token usage can be included when they affect the deployment objective. The implementation can record the result and compare it with the acceptance criteria. The chosen prompt and evaluation results should be versioned together. Future model or prompt changes can then run the same regression set. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 7,
    "question": "Several services share a production prompt. The team needs reproducible behavior and rapid rollback after a regression. Which configuration design best fits? Let every service edit its own prompt independently, without recording which text produced each evaluation result or production response. Reviewers would evaluate whether this can version prompts and preserve rollback evidence.",
    "options": [
      "Let every service edit its own prompt independently, without recording which text produced each evaluation result or production response. Reviewers would evaluate whether this can version prompts and preserve rollback evidence.",
      "Publish immutable prompt versions with evaluations and switch production through a reversible release pointer. The design would use this reasoning to version prompts and preserve rollback evidence.",
      "Embed one prompt permanently in each binary and discard evaluation records after every deployment cycle. Reviewers would evaluate whether this can version prompts and preserve rollback evidence.",
      "Fetch the newest unreviewed prompt on every request so changes reach production without a release decision. The design would use this reasoning to version prompts and preserve rollback evidence."
    ],
    "correctAnswers": [
      "Publish immutable prompt versions with evaluations and switch production through a reversible release pointer. The design would use this reasoning to version prompts and preserve rollback evidence."
    ],
    "explanation": "Immutable prompt versions give each behavior change a stable identity. Linking evaluations to that identity shows which version satisfied the release criteria. In this scenario, that is what allows the team to version prompts and preserve rollback evidence. A production pointer can move deliberately to the approved version and return quickly to the prior one. All services can resolve the same selected version without editing prompt text in place. This makes the expected behavior easier to measure under production-like conditions. The release record should include prompt version, evaluation suite version, decision, and deployment time. A rollback exercise can verify that changing the pointer restores the previous prompt consistently. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 8,
    "question": "A team considers installing a third-party Claude Code plugin that requests broad filesystem access. Reviewers need enough evidence to approve the integration and detect failures before users are affected. What should they do before organization-wide rollout?",
    "options": [
      "Review its source, publisher, requested permissions, dependencies, and version, then record approval before organization-wide deployment. The team would then review plugin provenance, permissions, and dependencies based on that assumption.",
      "Install the plugin immediately because marketplace publication guarantees trusted code, dependencies, and least-privilege behavior for every release. The implementation would treat this as the rule for how to review plugin provenance, permissions, and dependencies.",
      "Copy the marketplace description into CLAUDE.md and treat it as a complete security assessment. The design would use this reasoning to review plugin provenance, permissions, and dependencies.",
      "Grant the plugin broader filesystem access than requested so later versions never need another permission review. The design would use this reasoning to review plugin provenance, permissions, and dependencies."
    ],
    "correctAnswers": [
      "Review its source, publisher, requested permissions, dependencies, and version, then record approval before organization-wide deployment. The team would then review plugin provenance, permissions, and dependencies based on that assumption."
    ],
    "explanation": "Source, publisher, permission, dependency, and version review addresses both behavior and supply-chain risk. Recording approval creates an auditable organization decision before broad deployment. This gives the team a practical way to review plugin provenance, permissions, and dependencies. The approved release can be pinned or constrained according to the team's update policy. New versions can then be evaluated against the same criteria before replacing it. It also creates an implementation boundary that can be inspected and tested. A pilot should verify the plugin in a restricted environment and observe the filesystem operations it actually needs. Organization controls can allow only the reviewed marketplace and release after validation. The team can compare later releases against the same operational standard."
  },
  {
    "number": 9,
    "question": "A TypeScript service uses the official SDK. Reviewers want to avoid an optimization that hides the actual cause of the production behavior. Which replacement correctly waits for the Messages request before reading its response object? const pending = client.messages.create({ model: selectedModel, max_tokens: 256, messages: [{ role: \"user\", content: prompt }] }); console.log(pending.content); Call JSON.stringify on the unresolved promise before directly accessing its content field asynchronously. Reviewers would evaluate whether this can await the SDK request before reading results. Wrap the client in a new WebSocket connection and wait for a socket close event. The application would follow this approach to await the SDK request before reading results.",
    "options": [
      "Call JSON.stringify on the unresolved promise before directly accessing its content field asynchronously. Reviewers would evaluate whether this can await the SDK request before reading results.",
      "Wrap the client in a new WebSocket connection and wait for a socket close event. The application would follow this approach to await the SDK request before reading results.",
      "Await client.messages.create, then inspect the resolved message content blocks. Reviewers would evaluate whether this can await the SDK request before reading results.",
      "Read pending.content directly because the SDK blocks until each response has completed. The team would then await the SDK request before reading results based on that assumption."
    ],
    "correctAnswers": [
      "Await client.messages.create, then inspect the resolved message content blocks. Reviewers would evaluate whether this can await the SDK request before reading results."
    ],
    "explanation": "Awaiting client.messages.create yields the completed message response. The application can then inspect its typed content blocks and other metadata. In this scenario, that is what allows the team to await the SDK request before reading results. This follows normal TypeScript promise semantics and the official SDK examples. It also keeps network completion separate from response parsing. This makes the expected behavior easier to measure under production-like conditions. Production code should additionally handle request errors and verify expected content block types. Those checks occur after the promise resolves or rejects. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 10,
    "question": "Claude follows an incident-summary instruction inconsistently despite clear field names. Any revision must be evaluated against the same quality and latency criteria before release. Which prompt change most directly demonstrates the required pattern?",
    "options": [
      "Provide representative input-output examples that show the exact fields and acceptable values. This would become the basis used to produce a consistent incident-summary pattern.",
      "Repeat the same instruction in uppercase at the beginning and end of the prompt. The design would use this reasoning to produce a consistent incident-summary pattern.",
      "Remove the field constraints and let each response choose a format that fits its content. The implementation would treat this as the rule for how to produce a consistent incident-summary pattern.",
      "Add unrelated background documents so Claude has more tokens available for deciding the format. This would become the basis used to produce a consistent incident-summary pattern."
    ],
    "correctAnswers": [
      "Provide representative input-output examples that show the exact fields and acceptable values. This would become the basis used to produce a consistent incident-summary pattern."
    ],
    "explanation": "Representative examples show Claude how inputs map to the expected fields and values. This is few-shot prompting, which is useful when instructions alone do not establish consistent behavior. In this scenario, that is what allows the team to produce a consistent incident-summary pattern. Examples should cover realistic cases and remain aligned with the written instruction. Contradictory or unrepresentative demonstrations can teach the wrong pattern. This makes the expected behavior easier to measure under production-like conditions. The team should test the revised prompt on held-out incidents. That evaluation reveals whether the examples improve consistency beyond the examples themselves. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 11,
    "question": "A development team is reviewing this behavior before approving the production implementation. The team wants a production design that can be tested, monitored, and supported without changing the stated behavior. What is the primary problem with the following messages portion of a Messages API request? { \"messages\": [ {\"role\": \"system\", \"content\": \"Be concise.\"}, {\"role\": \"user\", \"content\": \"Summarize this.\"} ] } The request needs two user turns before Claude can generate any assistant response at all. Reviewers would evaluate whether this can place system instructions in the correct field. The user content must be an image block because text content is unavailable in Messages requests. Reviewers would evaluate whether this can place system instructions in the correct field.",
    "options": [
      "The request needs two user turns before Claude can generate any assistant response at all. Reviewers would evaluate whether this can place system instructions in the correct field.",
      "The user content must be an image block because text content is unavailable in Messages requests. Reviewers would evaluate whether this can place system instructions in the correct field.",
      "The request puts a starting instruction in messages; instructions that apply from beginning belong in top-level system. This would become the basis used to place system instructions in the correct field.",
      "The request must begin with assistant content because prefilling is mandatory for every model. The design would use this reasoning to place system instructions in the correct field."
    ],
    "correctAnswers": [
      "The request puts a starting instruction in messages; instructions that apply from beginning belong in top-level system. This would become the basis used to place system instructions in the correct field."
    ],
    "explanation": "An instruction that applies from the start belongs in the request's top-level system field. Placing system as the first entry in messages is not the documented starting-instruction shape. This is the mechanism the team needs to place system instructions in the correct field. Some current models support a system-role message partway through a conversation under specific placement rules. That exception requires the message to follow a user turn and does not validate the first entry shown here. The team can verify the behavior with explicit checks before release. Moving Be concise to top-level system leaves the user turn in messages and expresses the intended scope directly. The application should still follow the feature support of its selected model. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 12,
    "question": "A team is choosing between Anthropic's official SDK and direct REST calls for two services. The team must interpret the behavior correctly before changing model settings or request handling. Which TWO implementation statements are correct? Wrap REST JSON in SDK classes before sending every HTTP request. This would become the basis used to choose correctly between SDK and REST implementations.",
    "options": [
      "Wrap REST JSON in SDK classes before sending every HTTP request. This would become the basis used to choose correctly between SDK and REST implementations.",
      "For raw REST, send documented headers and JSON to the Messages endpoint. The team would apply the same decision when it must choose correctly between SDK and REST implementations.",
      "Use the official SDK for typed, idiomatic request objects and language-native response handling in code. The selected method would be used to choose correctly between SDK and REST implementations before release.",
      "Assume SDK and raw REST response objects are identical, including all language-specific helper methods. Reviewers would evaluate whether this can choose correctly between SDK and REST implementations.",
      "Omit the API-version header because raw REST compatibility follows the JSON body automatically. The design would use this reasoning to choose correctly between SDK and REST implementations."
    ],
    "correctAnswers": [
      "Use the official SDK for typed, idiomatic request objects and language-native response handling in code. The selected method would be used to choose correctly between SDK and REST implementations before release.",
      "For raw REST, send documented headers and JSON to the Messages endpoint. The team would apply the same decision when it must choose correctly between SDK and REST implementations."
    ],
    "explanation": "Official client SDKs provide idiomatic interfaces and type safety for their supported languages. Those interfaces reduce manual construction of request objects and response handling code. The important point is that the design can choose correctly between SDK and REST implementations. The SDK maps language-native calls to the Messages API contract. Developers can work with the conventions of their language while still controlling each request. The implementation can record the result and compare it with the acceptance criteria. Using an SDK does not remove the need for application validation and policy checks. It supplies a maintained integration surface rather than business correctness guarantees. The same evidence can support monitoring, rollback, and later revisions. A raw REST client can call the Messages endpoint directly with a JSON request body. It must send the documented authentication, API-version, and content-type headers itself. This is the mechanism the team needs to choose correctly between SDK and REST implementations. Direct HTTP avoids a language SDK dependency but transfers more protocol responsibility to the application. The implementation must parse responses and errors according to the API contract. The team can verify the behavior with explicit checks before release. Both integration styles ultimately call the same service behavior. The correct choice depends on runtime constraints and how much client plumbing the team wants to own. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 13,
    "question": "A maintenance assistant must interpret open-ended incident evidence and propose actions, but every executed action must satisfy a machine-enforced target allowlist and change window. The design must balance model reasoning with deterministic controls and observable execution. Which architecture best fits? Have peer agents vote on action, then execute the majority choice without checking policy against trusted state. The team would then enforce policy before any maintenance execution based on that assumption. Let Claude both invent and approve maintenance actions because one model decision keeps control centralized. The selected method would be used to enforce policy before any maintenance execution.",
    "options": [
      "Have peer agents vote on action, then execute the majority choice without checking policy against trusted state. The team would then enforce policy before any maintenance execution based on that assumption.",
      "Let Claude both invent and approve maintenance actions because one model decision keeps control centralized. The selected method would be used to enforce policy before any maintenance execution.",
      "Let Claude propose maintenance actions, then pass each proposal through deterministic policy validation before executor runs. The team would then enforce policy before any maintenance execution based on that assumption before release.",
      "Put policy text in the prompt and allow the agent to call executor directly. This would become the basis used to enforce policy before any maintenance execution."
    ],
    "correctAnswers": [
      "Let Claude propose maintenance actions, then pass each proposal through deterministic policy validation before executor runs. The team would then enforce policy before any maintenance execution based on that assumption before release."
    ],
    "explanation": "Claude can interpret the variable incident evidence and formulate a candidate action. A deterministic validator can then evaluate the target and timing against trusted policy state. The important point is that the design can enforce policy before any maintenance execution. Separating proposal from execution prevents model judgment from becoming the authorization boundary. The executor receives only actions that pass the application-owned checks. The implementation can record the result and compare it with the acceptance criteria. The design also creates an auditable record of the proposal, policy decision, and executed action. Those records let operators distinguish model behavior from enforcement behavior during review. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 14,
    "question": "An agent summarizes supplier web pages and can draft payments through a finance tool. A page may contain hidden instructions that attempt to override the system prompt and submit a payment. The application must preserve useful summarization while preventing untrusted content from causing a consequential action. Which TWO actions should the developer take?",
    "options": [
      "Place retrieved page content in a clearly delimited untrusted-data section and instruct Claude not to treat it as authority. The selected method would be used to resist injection and require payment authorization.",
      "Increase temperature so hidden instructions are less likely to produce the same action repeatedly. The selected method would be used to resist injection and require payment authorization.",
      "Move the page text into the system prompt so it receives the strongest instruction priority. The selected method would be used to resist injection and require payment authorization.",
      "Require deterministic authorization and human approval before any payment tool call can execute. This would become the basis used to resist injection and require payment authorization.",
      "Give the finance tool read and write access so Claude can correct a malicious payment automatically. The team would then resist injection and require payment authorization based on that assumption."
    ],
    "correctAnswers": [
      "Place retrieved page content in a clearly delimited untrusted-data section and instruct Claude not to treat it as authority. The selected method would be used to resist injection and require payment authorization.",
      "Require deterministic authorization and human approval before any payment tool call can execute. This would become the basis used to resist injection and require payment authorization."
    ],
    "explanation": "The page is third-party content and therefore an indirect prompt-injection source. Marking and structuring it as untrusted data helps Claude distinguish content to summarize from instructions it may follow. In this scenario, that is what allows the team to resist injection and require payment authorization. Trusted policy remains in the system or application-controlled instruction layer. The retrieved page cannot legitimately replace that policy merely because it contains imperative language. This makes the expected behavior easier to measure under production-like conditions. This preserves the useful summarization path while reducing instruction-priority confusion. The application should also retain the source and trust label for monitoring and testing. That makes the design easier to audit and maintain over time. Authorization must be enforced outside the model before the finance action executes. Human approval creates a deliberate checkpoint for a consequential payment. That directly supports the need to resist injection and require payment authorization. The finance capability should receive only the permissions required for the approved operation. A successful injection then cannot silently convert summarization into an unrestricted payment. It gives reviewers concrete evidence instead of relying on an informal assumption. This layer remains effective even if the model proposes a harmful tool call. It protects the real-world action rather than relying only on the model to recognize the attack. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 15,
    "question": "A team needs durable state, explicit transition rules, resumable human approvals, and visual inspection of every workflow branch. The workflow should use model discretion only where it adds value to the stated task. Which approach best fits? Launch independent agents for each branch without recording dependencies, completed transitions, or approval state. The team would then persist state, approvals, and resumable checkpoints based on that assumption.",
    "options": [
      "Launch independent agents for each branch without recording dependencies, completed transitions, or approval state. The team would then persist state, approvals, and resumable checkpoints based on that assumption.",
      "Use a state-graph framework that models transitions, durable checkpoints, approval pauses, and resumable execution as state. This would become the basis used to persist state, approvals, and resumable checkpoints.",
      "Use a stateless tool loop and reconstruct prior workflow decisions from logs after an interruption. This would become the basis used to persist state, approvals, and resumable checkpoints.",
      "Use one prompt that asks Claude to remember stages, approvals, and branch state without durable checkpoints or transitions. Reviewers would evaluate whether this can persist state, approvals, and resumable checkpoints."
    ],
    "correctAnswers": [
      "Use a state-graph framework that models transitions, durable checkpoints, approval pauses, and resumable execution as state. This would become the basis used to persist state, approvals, and resumable checkpoints."
    ],
    "explanation": "A state graph represents each stage and allowed transition explicitly. Durable checkpoints preserve the current node and relevant state across interruption. That directly supports the need to persist state, approvals, and resumable checkpoints. An approval pause can be modeled as a transition that waits for an external decision. Resumption then follows the recorded branch instead of relying on model memory. It gives reviewers concrete evidence instead of relying on an informal assumption. The graph also gives reviewers a visible representation of possible paths and terminal states. Framework selection is justified here by the required durability and inspection features. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 16,
    "question": "A classification service sends the same request to Claude several times and occasionally receives different labels. The decision should be based on observable model behavior, token usage, and documented API semantics. Which explanation best reflects an LLM fundamental? The SDK automatically rotates across model families after each successful request to balance regional service availability. This would become the basis used to guide prompt tuning and response validation. The Messages API silently rewrites identical user prompts whenever the API key is reused. The team would then guide prompt tuning and response validation based on that assumption. Claude stores each prior label permanently and deliberately avoids returning that label again. The design would use this reasoning to guide prompt tuning and response validation.",
    "options": [
      "The SDK automatically rotates across model families after each successful request to balance regional service availability. This would become the basis used to guide prompt tuning and response validation.",
      "The Messages API silently rewrites identical user prompts whenever the API key is reused. The team would then guide prompt tuning and response validation based on that assumption.",
      "Claude stores each prior label permanently and deliberately avoids returning that label again. The design would use this reasoning to guide prompt tuning and response validation.",
      "Claude generates tokens probabilistically, so repeated calls can vary even when the input remains unchanged. The team would apply the same decision when it must guide prompt tuning and response validation."
    ],
    "correctAnswers": [
      "Claude generates tokens probabilistically, so repeated calls can vary even when the input remains unchanged. The team would apply the same decision when it must guide prompt tuning and response validation."
    ],
    "explanation": "Large language models generate responses through next-token prediction rather than a deterministic lookup table. Multiple valid continuations can therefore produce different final labels across otherwise similar calls. In this scenario, that is what allows the team to guide prompt tuning and response validation. The variation does not automatically indicate a transport or SDK defect. An application should judge whether outputs meet the task criteria across a representative evaluation set. This makes the expected behavior easier to measure under production-like conditions. For a classification workflow, teams can strengthen instructions, add examples, and validate the returned label. They should still measure consistency instead of assuming identical outputs are guaranteed. That makes the design easier to audit and maintain over time. via - https://platform.claude.com/docs/en/test-and-evaluate/strengthen-guardrails/increase-consistency"
  },
  {
    "number": 17,
    "question": "A team's monthly Claude cost is higher than its spreadsheet forecast. The decision should be based on observable model behavior, token usage, and documented API semantics. Which investigation best validates the cost model?",
    "options": [
      "Aggregate actual input, output, cache, and request usage by workload, then compare current pricing assumptions. The selected method would be used to reconcile token usage with current model pricing during release review.",
      "Assume the invoice is incorrect whenever generated responses are shorter than user prompts. The implementation would treat this as the rule for how to reconcile token usage with current model pricing.",
      "Lower max_tokens globally before identifying which workload or token category caused the difference. The selected method would be used to reconcile token usage with current model pricing.",
      "Estimate all requests from the median character count and ignore response usage fields. The application would follow this approach to reconcile token usage with current model pricing."
    ],
    "correctAnswers": [
      "Aggregate actual input, output, cache, and request usage by workload, then compare current pricing assumptions. The selected method would be used to reconcile token usage with current model pricing during release review."
    ],
    "explanation": "Usage data shows the token categories and request volumes that actually occurred. Segmenting by workload reveals whether a particular route, prompt, or output pattern drove the increase. The option therefore enables the implementation to reconcile token usage with current model pricing. Cache reads and cache writes can have different pricing effects from ordinary input tokens. The model should represent those categories rather than collapsing every input into one rate. The resulting control can be exercised repeatedly during regression testing. After reconciling observed usage with current pricing, the team can test targeted optimizations. This sequence separates measurement error from a real workload change. Future changes can be reviewed without losing the original reasoning."
  },
  {
    "number": 18,
    "question": "A support classifier must return exactly one category and a bounded confidence value. The implementation must preserve the required data and execution boundaries under production conditions. Which TWO design changes improve enforceability? { \"type\": \"object\", \"properties\": { \"category\": {\"type\": \"string\"}, \"confidence\": {\"type\": \"number\"} } } Replace confidence with free-form explanatory text that preserves no numeric validation contract whatsoever. The design would use this reasoning to enforce category values and confidence bounds. Accept any category string and rely on reviewers to notice labels unsupported by the downstream application. The design would use this reasoning to enforce category values and confidence bounds.",
    "options": [
      "Replace confidence with free-form explanatory text that preserves no numeric validation contract whatsoever. The design would use this reasoning to enforce category values and confidence bounds.",
      "Accept any category string and rely on reviewers to notice labels unsupported by the downstream application. The design would use this reasoning to enforce category values and confidence bounds.",
      "Describe confidence in prose while accepting every numeric value, including values outside the application's documented scoring interval during validation. The design would use this reasoning to enforce category values and confidence bounds.",
      "Require both properties, then validate confidence bounds after parsing, including SDK post-validation when applicable. The selected method would be used to enforce category values and confidence bounds.",
      "Constrain category with an enum containing only labels that the application can recognize and process. This would become the basis used to enforce category values and confidence bounds."
    ],
    "correctAnswers": [
      "Constrain category with an enum containing only labels that the application can recognize and process. This would become the basis used to enforce category values and confidence bounds.",
      "Require both properties, then validate confidence bounds after parsing, including SDK post-validation when applicable. The selected method would be used to enforce category values and confidence bounds."
    ],
    "explanation": "An enum restricts category to the finite labels implemented by the support application. That constraint turns an informal naming convention into a schema rule. The option therefore enables the implementation to enforce category values and confidence bounds. Downstream routing can handle every accepted value without a fallback for invented categories. Enum values should be selected and compared with the documented casing behavior in mind. The resulting control can be exercised repeatedly during regression testing. Schema tests should accept each supported label and reject a representative unsupported value. Application tests should also map every enum member to a real processing branch. Future changes can be reviewed without losing the original reasoning. Listing both names in required prevents a structurally valid response from omitting either output. Numeric range checks must occur after parsing because minimum and maximum are not constrained-decoding guarantees. This is the mechanism the team needs to enforce category values and confidence bounds. Application validation can reject confidence values outside the documented interval. When a supported SDK helper removes unsupported bounds before sending the schema, it validates the response against the original schema afterward. The team can verify the behavior with explicit checks before release. Boundary tests should cover both endpoints, values just outside them, and objects missing each required property. Those cases verify presence and range at the layers that actually enforce them. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 19,
    "question": "A developer discovers that a production Claude API key was committed to a public repository, and account usage now looks suspicious. Security decisions must rely on enforced permissions and validation rather than prompt text alone. What should the team do first? Move the exposed key into an ignored dotenv file, then continue using it after reviewing account usage. Reviewers would evaluate whether this can revoke the exposed key and rotate securely. Wait for evidence of misuse before changing the key, because rotation could interrupt traffic. The design would use this reasoning to revoke the exposed key and rotate securely. Delete the repository commit and keep using the existing key because removing its history prevents future unauthorized API requests. The selected method would be used to revoke the exposed key and rotate securely.",
    "options": [
      "Move the exposed key into an ignored dotenv file, then continue using it after reviewing account usage. Reviewers would evaluate whether this can revoke the exposed key and rotate securely.",
      "Wait for evidence of misuse before changing the key, because rotation could interrupt traffic. The design would use this reasoning to revoke the exposed key and rotate securely.",
      "Delete the repository commit and keep using the existing key because removing its history prevents future unauthorized API requests. The selected method would be used to revoke the exposed key and rotate securely.",
      "Revoke the key immediately, create a replacement, and store the new key in secret management. The team would then revoke the exposed key and rotate securely based on that assumption."
    ],
    "correctAnswers": [
      "Revoke the key immediately, create a replacement, and store the new key in secret management. The team would then revoke the exposed key and rotate securely based on that assumption."
    ],
    "explanation": "A public key must be treated as compromised because anyone who copied it may continue using it. Immediate revocation invalidates the exposed credential and stops future requests made with that key. The important point is that the design can revoke the exposed key and rotate securely. The replacement should be stored through a secrets-management mechanism and injected at runtime. This reduces the chance that the new credential will enter source history again. The implementation can record the result and compare it with the acceptance criteria. The team should also review usage and logs, but that investigation should not delay revocation. Containment comes first when a live production credential has been exposed. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 20,
    "question": "A research system must compare four independent vendors and then produce one recommendation using a shared rubric. Every autonomous step must remain inspectable and subject to the required operational controls. Which execution sequence is most appropriate? Assign vendors to one agent sequentially, then ask separate reviewers to repeat all research before synthesis. The team would then coordinate vendor research, scoring, and synthesis based on that assumption. Store vendor names in memory and wait for later sessions to produce the recommendation. The team would apply the same decision when it must coordinate vendor research, scoring, and synthesis.",
    "options": [
      "Assign vendors to one agent sequentially, then ask separate reviewers to repeat all research before synthesis. The team would then coordinate vendor research, scoring, and synthesis based on that assumption.",
      "Store vendor names in memory and wait for later sessions to produce the recommendation. The team would apply the same decision when it must coordinate vendor research, scoring, and synthesis.",
      "Have a supervisor delegate vendor investigations in parallel, then synthesize results against one shared rubric. The team would then coordinate vendor research, scoring, and synthesis based on that assumption during release review.",
      "Let independent researchers produce recommendations without a coordinator comparing evidence, assumptions, and scores under the shared rubric. The team would then coordinate vendor research, scoring, and synthesis based on that assumption."
    ],
    "correctAnswers": [
      "Have a supervisor delegate vendor investigations in parallel, then synthesize results against one shared rubric. The team would then coordinate vendor research, scoring, and synthesis based on that assumption during release review."
    ],
    "explanation": "A supervisor can assign each vendor to an isolated worker so the investigations proceed concurrently. Each worker can return a bounded evidence package instead of sharing an expanding research transcript. This is the mechanism the team needs to coordinate vendor research, scoring, and synthesis. via - https://platform.claude.com/docs/en/managed-agents/multiagent-orchestration The supervisor retains ownership of the common rubric and compares the returned findings consistently. That prevents four workers from applying incompatible scoring logic. The team can verify the behavior with explicit checks before release. Final synthesis is a dependency that begins after the investigations complete. This sequence combines parallel discovery with coordinated judgment. This keeps the production decision traceable when the system changes. via - https://www.anthropic.com/engineering/building-effective-agents"
  },
  {
    "number": 21,
    "question": "This Python function summarizes independent documents, but latency grows almost linearly with document count. The team wants a production design that can be tested, monitored, and supported without changing the stated behavior. Which change best improves throughput safely? async def summarize_all(client, docs): results = [] for doc in docs: results.append(await summarize(client, doc)) return results",
    "options": [
      "results.append(await summarize(client, doc))",
      "Run independent summaries with bounded concurrency, then preserve document order when collecting completed results. The design would use this reasoning to process independent documents concurrently within safe limits.",
      "Cache the first summary and return it for every document to eliminate additional network calls. The design would use this reasoning to process independent documents concurrently within safe limits.",
      "Remove async from summarize_all and call the same network requests synchronously inside the existing loop instead. Reviewers would evaluate whether this can process independent documents concurrently within safe limits.",
      "Launch every request simultaneously without any concurrency limit, timeout, cancellation policy, or handling for any partial service failures. Reviewers would evaluate whether this can process independent documents concurrently within safe limits."
    ],
    "correctAnswers": [
      "Run independent summaries with bounded concurrency, then preserve document order when collecting completed results. The design would use this reasoning to process independent documents concurrently within safe limits."
    ],
    "explanation": "The summaries are independent, so a limited number can run while earlier requests are waiting on the network. A concurrency bound prevents the function from creating an uncontrolled request burst. This is the mechanism the team needs to process independent documents concurrently within safe limits. Collecting results by original index preserves the caller-visible document order even if tasks finish differently. Timeouts and cancellation handling can be applied around each bounded task. The team can verify the behavior with explicit checks before release. Tests should vary completion order, inject one failed request, and assert both the concurrency ceiling and output mapping. Load tests can then select a limit that respects service rate and resource constraints. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 22,
    "question": "A team states only, “The system should extract invoice fields accurately and respond quickly.” The selected approach must produce clear request, response, and operational behavior across the workflow. Which TWO additions make the requirement testable? Let reviewers approve outputs informally whenever extracted fields appear reasonable during manual inspection sessions. The application would follow this approach to set measurable release gates for invoice extraction.",
    "options": [
      "Let reviewers approve outputs informally whenever extracted fields appear reasonable during manual inspection sessions. The application would follow this approach to set measurable release gates for invoice extraction.",
      "Specify a maximum end-to-end latency at a stated percentile, production load, and measurement window for the deployed service. Reviewers would evaluate whether this can set measurable release gates for invoice extraction.",
      "Define a labeled invoice set, field-level scoring method, and minimum accuracy threshold that must pass before release. Reviewers would evaluate whether this can set measurable release gates for invoice extraction.",
      "Require one invoice layout and exclude all intended variations from the evaluation set before release. The design would use this reasoning to set measurable release gates for invoice extraction.",
      "Select the strongest available model and treat its published capability description as the only acceptance threshold. The design would use this reasoning to set measurable release gates for invoice extraction."
    ],
    "correctAnswers": [
      "Define a labeled invoice set, field-level scoring method, and minimum accuracy threshold that must pass before release. Reviewers would evaluate whether this can set measurable release gates for invoice extraction.",
      "Specify a maximum end-to-end latency at a stated percentile, production load, and measurement window for the deployed service. Reviewers would evaluate whether this can set measurable release gates for invoice extraction."
    ],
    "explanation": "A labeled invoice set defines the examples on which extraction quality will be judged. Field-level scoring identifies whether individual required values are correct rather than relying on a vague overall impression. In this scenario, that is what allows the team to set measurable release gates for invoice extraction. A minimum threshold turns accuracy into a release decision. The same dataset, method, and threshold can be applied to later revisions for regression testing. This makes the expected behavior easier to measure under production-like conditions. The evaluation should represent the intended invoice distribution and important edge cases. Otherwise a precise score could still describe the wrong workload. That makes the design easier to audit and maintain over time. via - https://platform.claude.com/docs/en/test-and-evaluate/strengthen-guardrails/reduce-latency A latency percentile defines which portion of requests must satisfy the bound. It is more operationally meaningful than an unspecified average or the word quickly. The option therefore enables the implementation to set measurable release gates for invoice extraction. The production load and measurement window establish the conditions under which latency is observed. That prevents a lightly loaded development test from standing in for deployed behavior. The resulting control can be exercised repeatedly during regression testing. Together the bound and conditions form a repeatable service acceptance criterion. The requirement remains independent of which model or implementation eventually meets it. Future changes can be reviewed without losing the original reasoning. via - https://platform.claude.com/docs/en/test-and-evaluate/eval-tool"
  },
  {
    "number": 23,
    "question": "An MCP server must expose a versioned policy catalog that clients can list and read by URI as context, plus a risk-scoring operation that accepts case details and performs a calculation. The developer wants the primitives to communicate whether each capability supplies data or performs an action. Which design should the developer use? Publish both capabilities as prompts because prompts can contain data and request calculations directly. The team would then separate policy resources from risk-scoring tools based on that assumption. Put the catalog inside the risk tool's description and expose no resource. This would become the basis used to separate policy resources from risk-scoring tools.",
    "options": [
      "Publish both capabilities as prompts because prompts can contain data and request calculations directly. The team would then separate policy resources from risk-scoring tools based on that assumption.",
      "Put the catalog inside the risk tool's description and expose no resource. This would become the basis used to separate policy resources from risk-scoring tools.",
      "Expose the catalog as a tool and store the risk-scoring operation in a resource URI. The team would apply the same decision when it must separate policy resources from risk-scoring tools.",
      "Expose the catalog as a resource and the risk-scoring operation as a tool. Reviewers would evaluate whether this can separate policy resources from risk-scoring tools."
    ],
    "correctAnswers": [
      "Expose the catalog as a resource and the risk-scoring operation as a tool. Reviewers would evaluate whether this can separate policy resources from risk-scoring tools."
    ],
    "explanation": "The catalog is data that clients must list and read by URI, which matches an MCP resource. The risk score is an executable calculation with case arguments, which matches an MCP tool. This is the mechanism the team needs to separate policy resources from risk-scoring tools. Resources provide application-controlled contextual data through discovery and read operations. Tools expose model-invokable operations with input schemas and call results. The team can verify the behavior with explicit checks before release. Using the primitives this way communicates intent and keeps data separate from executable behavior. The catalog and scoring operation can then evolve without overloading descriptions or prompt templates. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 24,
    "question": "Claude returns stop_reason tool_use and one client tool_use block. The team needs a schema and execution path that can be validated before any external action occurs. What should the application do next in the standard tool-use loop? Execute every available tool concurrently, then append all outputs to the original assistant message before resubmitting it. Reviewers would evaluate whether this can execute the client tool and return results before release. Send the identical assistant response back to Claude until stop_reason changes from tool_use to end_turn. The application would follow this approach to execute the client tool and return results.",
    "options": [
      "Execute every available tool concurrently, then append all outputs to the original assistant message before resubmitting it. Reviewers would evaluate whether this can execute the client tool and return results before release.",
      "Send the identical assistant response back to Claude until stop_reason changes from tool_use to end_turn. The application would follow this approach to execute the client tool and return results.",
      "Run the named tool, then return a user tool_result block that references the tool_use_id. This would become the basis used to execute the client tool and return results.",
      "Treat the tool_use block as final text and show its JSON arguments directly to the user. The application would follow this approach to execute the client tool and return results."
    ],
    "correctAnswers": [
      "Run the named tool, then return a user tool_result block that references the tool_use_id. This would become the basis used to execute the client tool and return results."
    ],
    "explanation": "For a client tool, the application owns execution after Claude returns a tool_use block. It should validate the request, run the named operation, and capture the result or error. That directly supports the need to execute the client tool and return results. The application then sends a user message containing a tool_result block tied to the original tool_use identifier. That association tells Claude which pending request the observation satisfies. It gives reviewers concrete evidence instead of relying on an informal assumption. A subsequent Messages API call lets Claude use the result, request another tool, or finish its answer. This repeating exchange forms the standard client-side tool loop. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 25,
    "question": "A new classifier passed offline tests but may behave differently on live traffic. Reviewers need enough evidence to approve the integration and detect failures before users are affected. Which next step best limits operational risk? Keep the classifier offline until every conceivable input and integration condition has been exhaustively tested. Reviewers would evaluate whether this can control staged rollout, monitoring, and rollback. Release globally, then define rollback thresholds only after users report harmful production behavior patterns. The design would use this reasoning to control staged rollout, monitoring, and rollback. Replace the production classifier globally because offline evaluations already prove behavior under every live traffic condition. The design would use this reasoning to control staged rollout, monitoring, and rollback.",
    "options": [
      "Keep the classifier offline until every conceivable input and integration condition has been exhaustively tested. Reviewers would evaluate whether this can control staged rollout, monitoring, and rollback.",
      "Release globally, then define rollback thresholds only after users report harmful production behavior patterns. The design would use this reasoning to control staged rollout, monitoring, and rollback.",
      "Replace the production classifier globally because offline evaluations already prove behavior under every live traffic condition. The design would use this reasoning to control staged rollout, monitoring, and rollback.",
      "Release to a small monitored cohort with predefined rollback thresholds, then expand only when live validation meets criteria. The selected method would be used to control staged rollout, monitoring, and rollback."
    ],
    "correctAnswers": [
      "Release to a small monitored cohort with predefined rollback thresholds, then expand only when live validation meets criteria. The selected method would be used to control staged rollout, monitoring, and rollback."
    ],
    "explanation": "A limited cohort bounds exposure while the team observes the classifier on real integrations and traffic. Monitoring can compare live quality and operational metrics with the offline baseline. The important point is that the design can control staged rollout, monitoring, and rollback. Predefined rollback thresholds turn observed regressions into an immediate release decision. The team does not need to invent its tolerance after users are already affected. The implementation can record the result and compare it with the acceptance criteria. Expansion should occur only when the monitored evidence satisfies the stated criteria. This creates a controlled sequence from offline validation to broader production use. The same evidence can support monitoring, rollback, and later revisions. via - https://platform.claude.com/docs/en/test-and-evaluate/develop-tests"
  },
  {
    "number": 26,
    "question": "An application compares two contracts and must preserve which document supports each finding. The selected approach must produce clear request, response, and operational behavior across the workflow. Which TWO message-design choices best maintain source identity? Upload only the first contract and ask Claude to infer differences from knowledge. This would become the basis used to preserve source identity for every finding.",
    "options": [
      "Upload only the first contract and ask Claude to infer differences from knowledge. This would become the basis used to preserve source identity for every finding.",
      "Send each contract in a separate document block with a stable title that identifies its source. Reviewers would evaluate whether this can preserve source identity for every finding.",
      "Paste both contracts into one text string without preserving document names, boundaries, or source identity. The design would use this reasoning to preserve source identity for every finding.",
      "Convert every page into one unlabeled image stream, interleave unrelated instructions, and omit any mapping between pages and contract sources. The design would use this reasoning to preserve source identity for every finding.",
      "Place the comparison question after document blocks and request source-specific citations in the answer. The selected method would be used to preserve source identity for every finding."
    ],
    "correctAnswers": [
      "Send each contract in a separate document block with a stable title that identifies its source. Reviewers would evaluate whether this can preserve source identity for every finding.",
      "Place the comparison question after document blocks and request source-specific citations in the answer. The selected method would be used to preserve source identity for every finding."
    ],
    "explanation": "Separate document blocks preserve the two source boundaries in the request. Stable titles give Claude and the application an identifier for each contract. The option therefore enables the implementation to preserve source identity for every finding. The block structure remains useful when the contracts contain similar clause numbering or repeated language. Findings can refer to the intended title instead of an ambiguous combined text. The resulting control can be exercised repeatedly during regression testing. Request tests should swap document order while keeping titles fixed. Correct attribution after the swap demonstrates that source identity does not depend on position alone. Future changes can be reviewed without losing the original reasoning. Putting the question after the documents lets the request establish source content before asking for analysis. Source-specific citations make the attribution requirement explicit in the desired answer. The important point is that the design can preserve source identity for every finding. Claude citation support can tie cited passages to document content and metadata. The application can then render findings with the contract title that supports each statement. The implementation can record the result and compare it with the acceptance criteria. Validation should include one clause present in only one contract and another that conflicts across both. The expected result must cite the correct titled source for each finding. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 27,
    "question": "An application receives the following response and the final sentence is incomplete. The selected approach must balance quality, cost, latency, and predictable response handling. What should the application conclude first? { \"content\": [{\"type\": \"text\", \"text\": \"The policy requires\"}], \"stop_reason\": \"max_tokens\", \"usage\": {\"input_tokens\": 920, \"output_tokens\": 80} } The model completed naturally, and therefore the unfinished sentence is definitely the intended final answer. The application would follow this approach to detect incomplete output and request continuation. The SDK failed to deserialize a tool result and substituted partial text instead. The selected method would be used to detect incomplete output and request continuation. The input exceeded the context window, so the API discarded every earlier system and user instruction. Reviewers would evaluate whether this can detect incomplete output and request continuation before release.",
    "options": [
      "The model completed naturally, and therefore the unfinished sentence is definitely the intended final answer. The application would follow this approach to detect incomplete output and request continuation.",
      "The SDK failed to deserialize a tool result and substituted partial text instead. The selected method would be used to detect incomplete output and request continuation.",
      "The input exceeded the context window, so the API discarded every earlier system and user instruction. Reviewers would evaluate whether this can detect incomplete output and request continuation before release.",
      "Generation reached the configured output limit, so the text should be treated as incomplete. The team would then detect incomplete output and request continuation based on that assumption."
    ],
    "correctAnswers": [
      "Generation reached the configured output limit, so the text should be treated as incomplete. The team would then detect incomplete output and request continuation based on that assumption."
    ],
    "explanation": "A max_tokens stop reason means generation reached the requested or applicable output limit. The returned text may therefore end before Claude finishes the intended response. That directly supports the need to detect incomplete output and request continuation. The application should not present or parse the fragment as a complete answer. It can increase the output allowance where appropriate or continue through a documented recovery path. It gives reviewers concrete evidence instead of relying on an informal assumption. Recovery should still respect the total context and cost budget. The developer should validate the next response before allowing downstream use. This also reduces ambiguity when the team investigates a regression. via - https://platform.claude.com/docs/en/build-with-claude/handling-stop-reasons"
  },
  {
    "number": 28,
    "question": "A support agent should remember a customer's communication preference across separate sessions without retaining raw transcripts. Every autonomous step must remain inspectable and subject to the required operational controls. What design best fits? Replay full transcripts in every session, retaining unrelated customer details merely to recover one communication preference. The application would follow this approach to remember preferences without retaining raw transcripts.",
    "options": [
      "Replay full transcripts in every session, retaining unrelated customer details merely to recover one communication preference. The application would follow this approach to remember preferences without retaining raw transcripts.",
      "Place every customer's preferences in one shared memory record available to all support sessions. The design would use this reasoning to remember preferences without retaining raw transcripts.",
      "Store the validated preference in customer-scoped memory, and begin each support session with a fresh conversational context. The application would follow this approach to remember preferences without retaining raw transcripts.",
      "Expect Claude to recall earlier sessions without attaching saved memory or resupplying relevant prior state. Reviewers would evaluate whether this can remember preferences without retaining raw transcripts."
    ],
    "correctAnswers": [
      "Store the validated preference in customer-scoped memory, and begin each support session with a fresh conversational context. The application would follow this approach to remember preferences without retaining raw transcripts."
    ],
    "explanation": "Customer-scoped memory preserves the validated preference beyond one session without importing a full transcript. The scope ties the durable fact to the correct customer. This is the mechanism the team needs to remember preferences without retaining raw transcripts. Starting with fresh conversational context avoids carrying stale task details into a new support interaction. The application can retrieve only the preference needed for the current session. The team can verify the behavior with explicit checks before release. Validation matters because persistent memory can amplify an incorrect inference across later sessions. The service should store the confirmed preference and retain provenance appropriate to its policy. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 29,
    "question": "A repository has a stable release-review procedure expressed entirely as instructions. The team wants Claude Code to discover the procedure for matching tasks and load its details only when relevant. No external system access is needed. Which customization best fits? Add the instructions to every prompt so Claude always receives the complete procedure before each task. The selected method would be used to load reusable instructions only when relevant. Implement a custom API tool that returns the static procedure and requires application-side execution on every relevant task. Reviewers would evaluate whether this can load reusable instructions only when relevant. Deploy an MCP server that exposes the procedure through a remote tool and loads its description into every session. The selected method would be used to load reusable instructions only when relevant.",
    "options": [
      "Add the instructions to every prompt so Claude always receives the complete procedure before each task. The selected method would be used to load reusable instructions only when relevant.",
      "Implement a custom API tool that returns the static procedure and requires application-side execution on every relevant task. Reviewers would evaluate whether this can load reusable instructions only when relevant.",
      "Deploy an MCP server that exposes the procedure through a remote tool and loads its description into every session. The selected method would be used to load reusable instructions only when relevant.",
      "Create a project Skill that Claude can discover and load only for relevant tasks. The design would use this reasoning to load reusable instructions only when relevant."
    ],
    "correctAnswers": [
      "Create a project Skill that Claude can discover and load only for relevant tasks. The design would use this reasoning to load reusable instructions only when relevant."
    ],
    "explanation": "A project Skill stores specialized instructions with metadata that helps Claude decide when the capability is relevant. Claude can discover the Skill and load its detailed content when needed. The important point is that the design can load reusable instructions only when relevant. Project scope also allows the repository to share the procedure with the team. The guidance remains close to the codebase and can evolve through normal version control. The implementation can record the result and compare it with the acceptance criteria. This choice meets the requirement without adding an external execution surface. It also avoids placing the entire procedure into every unrelated prompt. The same evidence can support monitoring, rollback, and later revisions."
  },
  {
    "number": 30,
    "question": "A support platform has high-volume routine classification and rare policy appeals requiring deeper reasoning. The team must interpret the behavior correctly before changing model settings or request handling. Which model strategy is most defensible? Route by traffic volume, then review appeal quality after the next model release. The selected method would be used to route routine and complex cases cost effectively.",
    "options": [
      "Route by traffic volume, then review appeal quality after the next model release. The selected method would be used to route routine and complex cases cost effectively.",
      "Benchmark candidate tiers on representative evals, route by task needs, and retest before model changes. The application would follow this approach to route routine and complex cases cost effectively.",
      "Choose the least expensive tier that passes one aggregate benchmark across both task families. This would become the basis used to route routine and complex cases cost effectively.",
      "Route by published model descriptions and vendor benchmarks, then skip workload-specific evaluation to avoid delaying the production selection decision. The application would follow this approach to route routine and complex cases cost effectively."
    ],
    "correctAnswers": [
      "Benchmark candidate tiers on representative evals, route by task needs, and retest before model changes. The application would follow this approach to route routine and complex cases cost effectively."
    ],
    "explanation": "Representative evals make quality differences visible for both routine and difficult task families. Latency and cost measurements then support a routing policy grounded in actual requirements. This gives the team a practical way to route routine and complex cases cost effectively. The likely outcome is a faster, lower-cost tier for validated routine work and a stronger tier for appeals. That routing remains a hypothesis until the evaluation evidence supports it. It also creates an implementation boundary that can be inspected and tested. Model releases can change behavior even when the interface remains familiar. Retesting prompts and critical evals before migration protects the established quality threshold. The team can compare later releases against the same operational standard."
  },
  {
    "number": 31,
    "question": "A TypeScript service streams a Claude response containing a tool call. The service attempts to parse every input_json_delta.partial_json value as a complete JSON object, so it throws an error when the first fragment ends midway through a string. The developer wants to retain streaming while producing one valid tool input. Which implementation should the developer use? event: content_block_delta data: { \"delta\": { \"type\": \"input_json_delta\", \"partial_json\": \"{\\\"city\\\":\\\"San\" } }",
    "options": [
      "Concatenate the partial_json fragments in block order, then parse them after content_block_stop or use the SDK's incremental helpers. Reviewers would evaluate whether this can reconstruct streamed tool input before JSON parsing.",
      "Parse each partial_json fragment independently, then merge the resulting objects after message_stop arrives. The application would follow this approach to reconstruct streamed tool input before JSON parsing.",
      "Disable tool definitions during streaming so Claude returns the arguments as one ordinary text block. Reviewers would evaluate whether this can reconstruct streamed tool input before JSON parsing.",
      "Ignore input_json_delta events and reconstruct the tool arguments from the final text response. The selected method would be used to reconstruct streamed tool input before JSON parsing."
    ],
    "correctAnswers": [
      "Concatenate the partial_json fragments in block order, then parse them after content_block_stop or use the SDK's incremental helpers. Reviewers would evaluate whether this can reconstruct streamed tool input before JSON parsing."
    ],
    "explanation": "The partial_json value is explicitly a fragment, so concatenating fragments in block order preserves one logical tool input. Parsing after content_block_stop waits until the content block has finished. The option therefore enables the implementation to reconstruct streamed tool input before JSON parsing. Claude's streaming lifecycle opens a content block, emits one or more deltas, and then closes that block. The SDK can expose parsed incremental values, while a custom event loop must accumulate the fragment strings itself. The resulting control can be exercised repeatedly during regression testing. This approach retains streaming latency benefits without treating an incomplete string as a complete object. The completed input should still be validated against the tool schema before execution. Future changes can be reviewed without losing the original reasoning."
  },
  {
    "number": 32,
    "question": "A web application reuses one conversation history for all signed-in users. Users sometimes receive details from other sessions. What is the best correction? Add a prompt asking Claude to forget identities, while continuing to load the same history for every user. Reviewers would evaluate whether this can isolate conversation state for every user before release. Increase max_tokens so each response can include more of the shared cross-user conversation history content. The team would apply the same decision when it must isolate conversation state for every user.",
    "options": [
      "Add a prompt asking Claude to forget identities, while continuing to load the same history for every user. Reviewers would evaluate whether this can isolate conversation state for every user before release.",
      "Increase max_tokens so each response can include more of the shared cross-user conversation history content. The team would apply the same decision when it must isolate conversation state for every user.",
      "Store separate histories by authorized user and verify access before loading any conversation into a request. The design would use this reasoning to isolate conversation state for every user.",
      "Shuffle prior messages before each request so no single user's context remains consistently ordered. The implementation would treat this as the rule for how to isolate conversation state for every user."
    ],
    "correctAnswers": [
      "Store separate histories by authorized user and verify access before loading any conversation into a request. The design would use this reasoning to isolate conversation state for every user."
    ],
    "explanation": "Per-user storage creates an explicit boundary between conversation records. An authorization check ensures the requesting identity may load the selected history before it enters a Messages call. In this scenario, that is what allows the team to isolate conversation state for every user. The request should contain only the current user's relevant turns in the intended order. Server logs can record which authorized history identifier was loaded without copying other tenants' content. This makes the expected behavior easier to measure under production-like conditions. Isolation tests should create two accounts and attempt both legitimate and cross-account retrieval. The forbidden case must fail before any request is sent to Claude. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 33,
    "question": "A content-moderation assistant passes its overall offline score, but safety-critical false negatives remain concentrated in a small adversarial slice. The solution must keep authorization and safety controls outside untrusted model or content instructions. Which deployment plan best limits risk while the team improves the guardrails?",
    "options": [
      "Use a staged rollout with safety-slice thresholds, layered screening, production monitoring, and a documented and tested rollback path. Reviewers would evaluate whether this can apply slice-specific controls during gradual rollout.",
      "Release to all users immediately and rely on user complaints to identify harmful output patterns afterward. The application would follow this approach to apply slice-specific controls during gradual rollout.",
      "Keep the system offline until every possible adversarial input has been enumerated and blocked by rules. This would become the basis used to apply slice-specific controls during gradual rollout.",
      "Deploy globally after the average score passes, then disable detailed logging so production traffic cannot influence future safety evaluation results. The application would follow this approach to apply slice-specific controls during gradual rollout."
    ],
    "correctAnswers": [
      "Use a staged rollout with safety-slice thresholds, layered screening, production monitoring, and a documented and tested rollback path. Reviewers would evaluate whether this can apply slice-specific controls during gradual rollout."
    ],
    "explanation": "Safety-slice thresholds keep the known high-risk behavior visible instead of allowing the aggregate score to mask it. Staged rollout limits how many users encounter an unresolved failure while the team measures real behavior. In this scenario, that is what allows the team to apply slice-specific controls during gradual rollout. Layered screening addresses malicious or disallowed content at more than one point in the workflow. Production monitoring then detects regressions, new attack patterns, and differences from offline evaluation. This makes the expected behavior easier to measure under production-like conditions. A documented and tested rollback path lets the team respond quickly when the slice breaches its quality bar. Together these controls support evidence-driven expansion rather than an all-or-nothing launch. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 34,
    "question": "Claude returned a tool_use block with id toolu_123, but the next request receives a validation error. Reviewers need enough evidence to approve the integration and detect failures before users are affected. Which TWO defects are present? { \"role\": \"user\", \"content\": [ {\"type\": \"text\", \"text\": \"Thanks\"}, { \"type\": \"tool_result\", \"tool_use_id\": \"different_id\", \"content\": \"18 C\" } ] } The tool_result content is invalid because Messages accepts only nested JSON objects and never plain strings. Reviewers would evaluate whether this can match tool results to their tool-use identifiers. The message is invalid because every tool result requires an explicit temperature parameter. The design would use this reasoning to match tool results to their tool-use identifiers. The message must use assistant role because Claude initiated the preceding tool request itself. The team would apply the same decision when it must match tool results to their tool-use identifiers.",
    "options": [
      "The tool_result content is invalid because Messages accepts only nested JSON objects and never plain strings. Reviewers would evaluate whether this can match tool results to their tool-use identifiers.",
      "The message is invalid because every tool result requires an explicit temperature parameter. The design would use this reasoning to match tool results to their tool-use identifiers.",
      "The message must use assistant role because Claude initiated the preceding tool request itself. The team would apply the same decision when it must match tool results to their tool-use identifiers.",
      "The tool_result uses different_id instead of the identifier from Claude's preceding tool_use block, so correlation fails during validation. Reviewers would evaluate whether this can match tool results to their tool-use identifiers before release.",
      "The text block appears before tool_result, but tool results must come first in user content. The design would use this reasoning to match tool results to their tool-use identifiers."
    ],
    "correctAnswers": [
      "The tool_result uses different_id instead of the identifier from Claude's preceding tool_use block, so correlation fails during validation. Reviewers would evaluate whether this can match tool results to their tool-use identifiers before release.",
      "The text block appears before tool_result, but tool results must come first in user content. The design would use this reasoning to match tool results to their tool-use identifiers."
    ],
    "explanation": "A client tool result must reference the exact identifier from the tool_use block it satisfies. The artifact supplies different_id instead of toolu_123. The important point is that the design can match tool results to their tool-use identifiers. That mismatch prevents the API from correlating the observation with Claude's pending request. The application should preserve the tool-use identifier without transformation. The implementation can record the result and compare it with the acceptance criteria. Identifier validation should occur before the continuation request is sent. It can catch stale results, mixed concurrent calls, and accidental cross-request association. The same evidence can support monitoring, rollback, and later revisions. Tool-result blocks must appear before ordinary text blocks in the user content array. The artifact places Thanks first and the tool result second. In this scenario, that is what allows the team to match tool results to their tool-use identifiers. Moving tool_result to the beginning produces the documented continuation order. Optional user text can follow after all tool results. This makes the expected behavior easier to measure under production-like conditions. This ordering becomes especially important when one turn returns several tool results. The application should group those result blocks first before adding commentary. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 35,
    "question": "A long-running research assistant nears its context limit but must retain the user's goal, constraints, and accepted findings. The design should keep instructions, evidence, and requested output boundaries easy for Claude to distinguish. What should it do next? Delete the earliest turns without recording their decisions, then continue using only the most recent messages. The application would follow this approach to preserve goals and accepted findings across sessions. Move all historical tool outputs into the main system prompt so none can ever be removed later. Reviewers would evaluate whether this can preserve goals and accepted findings across sessions before release. Cache history and assume cached tokens no longer occupy the context window. The implementation would treat this as the rule for how to preserve goals and accepted findings across sessions.",
    "options": [
      "Delete the earliest turns without recording their decisions, then continue using only the most recent messages. The application would follow this approach to preserve goals and accepted findings across sessions.",
      "Move all historical tool outputs into the main system prompt so none can ever be removed later. Reviewers would evaluate whether this can preserve goals and accepted findings across sessions before release.",
      "Cache history and assume cached tokens no longer occupy the context window. The implementation would treat this as the rule for how to preserve goals and accepted findings across sessions.",
      "Compact earlier history into a summary, preserve key state, and verify continuity afterward. The team would apply the same decision when it must preserve goals and accepted findings across sessions."
    ],
    "correctAnswers": [
      "Compact earlier history into a summary, preserve key state, and verify continuity afterward. The team would apply the same decision when it must preserve goals and accepted findings across sessions."
    ],
    "explanation": "Compaction replaces detailed earlier context with a shorter summary that supports continued work. The summary should preserve the user's objective, constraints, confirmed facts, and unresolved questions. That directly supports the need to preserve goals and accepted findings across sessions. Critical state can also remain in explicit application memory rather than depending on an implicit conversation trace. This makes recovery and inspection more reliable. It gives reviewers concrete evidence instead of relying on an informal assumption. After compaction, a continuity check can confirm that the assistant still follows the accepted state. That validation catches omissions before further steps compound them. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 36,
    "question": "A TypeScript prompt builder produces weaker answers as the document collection grows. The team wants a prompt structure that remains clear as the context grows or changes over time. Which revision best follows long-context prompting guidance? const prompt = `Question: ${question}` + documents.join(\"\\n\") + \"\\nAnswer briefly.\"; Repeat the question between every document so it occupies most of the available context. The selected method would be used to place long documents for reliable retrieval.",
    "options": [
      "documents.join(\"\\n\") +",
      "\"\\nAnswer briefly.\";",
      "Repeat the question between every document so it occupies most of the available context. The selected method would be used to place long documents for reliable retrieval.",
      "Place delimited documents first, then put the specific question and response constraints after them. The team would apply the same decision when it must place long documents for reliable retrieval.",
      "Remove document boundaries and append every source into one long uninterrupted paragraph before producing the final answer. The team would apply the same decision when it must place long documents for reliable retrieval.",
      "Put the response constraints first and omit the question whenever documents are long. The implementation would treat this as the rule for how to place long documents for reliable retrieval."
    ],
    "correctAnswers": [
      "Place delimited documents first, then put the specific question and response constraints after them. The team would apply the same decision when it must place long documents for reliable retrieval."
    ],
    "explanation": "Anthropic recommends placing long documents near the top of a long-context prompt and the query after them. This ordering helps Claude attend to the evidence before answering the specific request. In this scenario, that is what allows the team to place long documents for reliable retrieval. Clear delimiters separate source material from instructions and reduce ambiguity about which text is data. XML-style tags are one practical option, but consistent descriptive boundaries are the key. This makes the expected behavior easier to measure under production-like conditions. The final question should still contain explicit output constraints and any citation requirement. The team should evaluate the revision on representative multi-document cases. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 37,
    "question": "A REST endpoint expects tags as an array of strings. The implementation must preserve the required data and execution boundaries under production conditions. Which defect appears in this payload? { \"title\": \"Quarterly report\", \"tags\": \"finance,review\", \"priority\": 2 }",
    "options": [
      "The tags property contains one comma-separated string instead of the required array containing separate string values. The application would follow this approach to validate request fields against the endpoint schema.",
      "The priority value must be quoted because REST request bodies permit strings but reject every numeric JSON value. The design would use this reasoning to validate request fields against the endpoint schema.",
      "The title is invalid because JSON strings cannot contain spaces between words in their values. Reviewers would evaluate whether this can validate request fields against the endpoint schema.",
      "The object needs trailing commas after each property to remain compatible with REST servers. The selected method would be used to validate request fields against the endpoint schema."
    ],
    "correctAnswers": [
      "The tags property contains one comma-separated string instead of the required array containing separate string values. The application would follow this approach to validate request fields against the endpoint schema."
    ],
    "explanation": "The contract requires an array, but the payload supplies one string containing a comma. A server cannot treat that scalar as two array elements without undocumented coercion. In this scenario, that is what allows the team to validate request fields against the endpoint schema. The corrected property would use a JSON array such as two separate string values. Contract tests should reject the displayed scalar and accept the array form. This makes the expected behavior easier to measure under production-like conditions. Matching JSON types matters independently of whether the text looks list-like to a person. The comma has no structural meaning while it remains inside quotation marks. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 38,
    "question": "A team repeatedly tells Claude Code the repository's test command, formatting rules, and architectural conventions. They want these instructions loaded for every session and shared through version control without applying them to unrelated repositories. What should they do? Add the conventions as permission allow rules because permissions provide persistent coding guidance for every session. The team would then load shared repository instructions in every session based on that assumption.",
    "options": [
      "Add the conventions as permission allow rules because permissions provide persistent coding guidance for every session. The team would then load shared repository instructions in every session based on that assumption.",
      "Commit a concise project-level CLAUDE.md containing the shared repository instructions. The team would apply the same decision when it must load shared repository instructions in every session.",
      "Store the instructions in the user-level CLAUDE.md so every local repository receives them. The application would follow this approach to load shared repository instructions in every session.",
      "Repeat the complete instruction set in the first prompt of every new session. The team would then load shared repository instructions in every session based on that assumption."
    ],
    "correctAnswers": [
      "Commit a concise project-level CLAUDE.md containing the shared repository instructions. The team would apply the same decision when it must load shared repository instructions in every session."
    ],
    "explanation": "A project-level CLAUDE.md is designed for persistent repository instructions that Claude Code loads into sessions. Committing it shares the same context with the team. In this scenario, that is what allows the team to load shared repository instructions in every session. Project scope keeps the instructions attached to this repository instead of every repository on a developer's machine. That matches the stated sharing and isolation requirements. This makes the expected behavior easier to measure under production-like conditions. The file should remain concise and contain commands, conventions, and architecture facts that developers would otherwise repeat. It guides Claude's behavior but is not a deterministic permission control. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 39,
    "question": "A service rejects some valid requests before calling Claude. Review the sizing logic. Which change most directly fixes the underlying problem? def fits_budget(text: str, limit: int) -> bool: estimated_tokens = len(text) // 4 return estimated_tokens < limit if not fits_budget(document, 80_000): raise ValueError(\"Prompt too large\")",
    "options": [
      "estimated_tokens = len(text) // 4",
      "if not fits_budget(document, 80_000):",
      "raise ValueError(\"Prompt too large\")",
      "Remove budget checking because Claude always truncates oversized inputs without an error. The implementation would treat this as the rule for how to admit requests using the actual token count.",
      "Call the token counting endpoint with the actual message structure before enforcing the budget. The selected method would be used to admit requests using the actual token count during release review.",
      "Count alphabetic characters because punctuation never contributes tokens to model input. The application would follow this approach to admit requests using the actual token count.",
      "Increase the character divisor until all rejected documents pass the local check. The selected method would be used to admit requests using the actual token count."
    ],
    "correctAnswers": [
      "Call the token counting endpoint with the actual message structure before enforcing the budget. The selected method would be used to admit requests using the actual token count during release review."
    ],
    "explanation": "The token counting endpoint accepts the same structured inputs used to create a message. It accounts for relevant request components more accurately than a character ratio. This is the mechanism the team needs to admit requests using the actual token count. The returned input count supports decisions about routing, cost, and context fit before generation. Anthropic notes that the count is still an estimate, but it is the purpose-built mechanism. The team can verify the behavior with explicit checks before release. The service should count the complete candidate request and reserve space for the intended output. It can then reject, trim, or summarize content according to an explicit policy. This keeps the production decision traceable when the system changes. via - https://platform.claude.com/docs/en/build-with-claude/token-counting"
  },
  {
    "number": 40,
    "question": "A Claude service classifies user-submitted support tickets. The classifier needs only the issue text, but the application sends full account histories and stores raw requests indefinitely in a broadly accessible analytics workspace. Which change most directly reduces leakage risk while preserving classification?",
    "options": [
      "Remove unneeded fields, redact identifiers, and send only the minimum ticket content required for classification. This would become the basis used to minimize data collection, retention, and access.",
      "Retain complete tickets and account histories in a restricted analytics workspace so future reviewers can investigate every classification decision. The application would follow this approach to minimize data collection, retention, and access.",
      "Encrypt every raw log at rest while continuing to send complete customer histories to Claude. This would become the basis used to minimize data collection, retention, and access.",
      "Add a system instruction telling Claude never to reveal personal data in classification responses. The design would use this reasoning to minimize data collection, retention, and access."
    ],
    "correctAnswers": [
      "Remove unneeded fields, redact identifiers, and send only the minimum ticket content required for classification. This would become the basis used to minimize data collection, retention, and access."
    ],
    "explanation": "Removing fields that are unnecessary for the classification task follows data minimization. Redacting identifiers further reduces the impact if prompts, traces, or outputs are exposed. That directly supports the need to minimize data collection, retention, and access. This change preserves the issue text that the classifier actually needs. It therefore reduces risk without discarding the evidence required to produce the requested category. It gives reviewers concrete evidence instead of relying on an informal assumption. The same design should be paired with appropriate retention, access control, and audit practices for any remaining data. Minimizing the request is the most direct control among the listed choices. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 41,
    "question": "A team chooses Claude Managed Agents with asynchronous sessions and an attached preference memory store. The workflow should use model discretion only where it adds value to the stated task. Which deployment characteristic does this choice most directly indicate? Managed sessions cannot expose tools because asynchronous execution supports only final text responses from Claude models. The design would use this reasoning to combine asynchronous sessions with durable preference memory. The application must run every loop inside its web process and independently build persistence, scheduling, isolation, and scaling. The application would follow this approach to combine asynchronous sessions with durable preference memory.",
    "options": [
      "Managed sessions cannot expose tools because asynchronous execution supports only final text responses from Claude models. The design would use this reasoning to combine asynchronous sessions with durable preference memory.",
      "The application must run every loop inside its web process and independently build persistence, scheduling, isolation, and scaling. The application would follow this approach to combine asynchronous sessions with durable preference memory.",
      "Anthropic hosts the managed agent harness, including asynchronous sessions and attached scoped memory store. This would become the basis used to combine asynchronous sessions with durable preference memory.",
      "The client connection must remain open until all long-running work finishes synchronously in one request. The application would follow this approach to combine asynchronous sessions with durable preference memory."
    ],
    "correctAnswers": [
      "Anthropic hosts the managed agent harness, including asynchronous sessions and attached scoped memory store. This would become the basis used to combine asynchronous sessions with durable preference memory."
    ],
    "explanation": "Claude Managed Agents supplies a harness that runs in Anthropic-managed infrastructure. The team does not need to keep its own request process alive for the entire asynchronous session. In this scenario, that is what allows the team to combine asynchronous sessions with durable preference memory. An attached memory store provides durable state that the managed session can access within its configured scope. Session execution and memory remain distinct resources with explicit identifiers. This makes the expected behavior easier to measure under production-like conditions. The application still starts work, observes status, and consumes results through the service interface. Managed hosting changes runtime ownership rather than eliminating application responsibilities. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 42,
    "question": "A product brief says the assistant must classify tickets and that the service must remain available during one regional outage. The team wants a production design that can be tested, monitored, and supported without changing the stated behavior. How should these requirements be classified? Classify both statements as functional requirements because each one affects the customer experience during service use. Reviewers would evaluate whether this can assign ownership and design separate acceptance tests. Classify both statements as infrastructure requirements because deployed architecture supports each capability in production. The design would use this reasoning to assign ownership and design separate acceptance tests. Treat ticket classification as infrastructure and regional fault tolerance as functional product behavior instead. The team would then assign ownership and design separate acceptance tests based on that assumption.",
    "options": [
      "Classify both statements as functional requirements because each one affects the customer experience during service use. Reviewers would evaluate whether this can assign ownership and design separate acceptance tests.",
      "Classify both statements as infrastructure requirements because deployed architecture supports each capability in production. The design would use this reasoning to assign ownership and design separate acceptance tests.",
      "Treat ticket classification as infrastructure and regional fault tolerance as functional product behavior instead. The team would then assign ownership and design separate acceptance tests based on that assumption.",
      "Treat ticket classification as functional behavior, and treat regional outage tolerance as a separate infrastructure availability constraint. The selected method would be used to assign ownership and design separate acceptance tests."
    ],
    "correctAnswers": [
      "Treat ticket classification as functional behavior, and treat regional outage tolerance as a separate infrastructure availability constraint. The selected method would be used to assign ownership and design separate acceptance tests."
    ],
    "explanation": "Ticket classification states the function the assistant must perform. Its acceptance criteria can evaluate whether intended tickets receive the required labels. In this scenario, that is what allows the team to assign ownership and design separate acceptance tests. Regional outage tolerance states an availability constraint that the service architecture must satisfy. It influences redundancy, routing, and failure handling rather than the classification prompt alone. This makes the expected behavior easier to measure under production-like conditions. Separating the categories lets the team validate product behavior and operational resilience with different tests. Both requirements remain important without being conflated. That makes the design easier to audit and maintain over time. via - https://platform.claude.com/docs/en/test-and-evaluate/develop-tests"
  },
  {
    "number": 43,
    "question": "A Claude Agent SDK TypeScript PreToolUse callback should block an unapproved deploy tool call, but the tool still executes. Every autonomous step must remain inspectable and subject to the required operational controls. What is the defect? const preToolUse = async (input: PreToolUseHookInput) => { const approved = (input.tool_input as { approved?: boolean }).approved; if (input.tool_name === \"deploy\" && !approved) { console.log(\"Denied\"); return {}; } return {}; }; The asynchronous callback cannot return permission decisions because only synchronous hooks can block tools. The team would then block unapproved deployments before tool execution based on that assumption. The callback compares lowercase deploy, but the SDK always converts every tool name to uppercase. The selected method would be used to block unapproved deployments before tool execution.",
    "options": [
      "if (input.tool_name === \"deploy\" && !approved) {",
      "The asynchronous callback cannot return permission decisions because only synchronous hooks can block tools. The team would then block unapproved deployments before tool execution based on that assumption.",
      "The callback compares lowercase deploy, but the SDK always converts every tool name to uppercase. The selected method would be used to block unapproved deployments before tool execution.",
      "The denied branch returns an empty object, so the runtime receives no permissionDecision of deny for execution. Reviewers would evaluate whether this can block unapproved deployments before tool execution before release.",
      "The callback reads approved from tool_input, but Agent SDK callbacks cannot inspect tool arguments before execution. The team would then block unapproved deployments before tool execution based on that assumption."
    ],
    "correctAnswers": [
      "The denied branch returns an empty object, so the runtime receives no permissionDecision of deny for execution. Reviewers would evaluate whether this can block unapproved deployments before tool execution before release."
    ],
    "explanation": "Returning an empty object tells the Agent SDK to allow the operation without changes. Logging Denied records an observation but supplies no enforcement decision. In this scenario, that is what allows the team to block unapproved deployments before tool execution. The branch should return hookSpecificOutput with the PreToolUse hook event and permissionDecision set to deny. A permissionDecisionReason can tell Claude why the action was blocked. This makes the expected behavior easier to measure under production-like conditions. That response prevents the deploy call before execution while preserving a clear trace. The ordinary empty return remains appropriate only for calls the policy allows. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 44,
    "question": "A pull request changes a response parser to handle text, tool calls, refusals, and truncation. The selected approach must produce clear request, response, and operational behavior across the workflow. Which evidence most directly supports a safe review decision?",
    "options": [
      "A monitoring plan records parser failures after release but includes no pre-merge cases for non-text content blocks. The design would use this reasoning to cover every documented response and stop path.",
      "One screenshot shows a text response succeeding on the developer's machine after the change once. Reviewers would evaluate whether this can cover every documented response and stop path.",
      "A reviewer says the new branch structure looks cleaner than the existing response parsing code overall. The application would follow this approach to cover every documented response and stop path.",
      "Table-driven tests verify handling for text, tool_use, refusal, and truncated response cases with assertions. The design would use this reasoning to cover every documented response and stop path."
    ],
    "correctAnswers": [
      "Table-driven tests verify handling for text, tool_use, refusal, and truncated response cases with assertions. The design would use this reasoning to cover every documented response and stop path."
    ],
    "explanation": "Table-driven tests make each response shape and expected handling result explicit. They directly exercise the non-text and terminal cases introduced by the parser change. That directly supports the need to cover every documented response and stop path. The fixtures can include representative content arrays and stop reasons from the Messages contract. Assertions then show whether the parser returns text, routes tool use, handles refusal, and detects truncation correctly. It gives reviewers concrete evidence instead of relying on an informal assumption. Running the table in continuous integration gives every reviewer the same evidence before merge. A later regression can be localized to the exact response case that changed. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 45,
    "question": "A team will replace a widely used Claude client wrapper across fifty services. Reviewers need enough evidence to approve the integration and detect failures before users are affected. Which TWO practices most reduce refactoring risk? interface ClaudeGateway { send(request: Request): Promise<Response>; } class LegacyGateway implements ClaudeGateway { async send(request: Request): Promise<Response> { return fetch(\"/claude\", { method: \"POST\", body: JSON.stringify(request) }); } }",
    "options": [
      "Allow each service to reinterpret response fields independently without a shared compatibility contract definition. Reviewers would evaluate whether this can replace the wrapper behind a stable interface.",
      "Preserve the gateway interface while introducing a new implementation behind the existing service contract for clients. Reviewers would evaluate whether this can replace the wrapper behind a stable interface.",
      "Delete the legacy implementation before testing parity, forcing every service to adopt the replacement during the first release. The design would use this reasoning to replace the wrapper behind a stable interface.",
      "Replace all fifty services and their tests together through one unreviewed migration release. The design would use this reasoning to replace the wrapper behind a stable interface.",
      "Run contract and regression tests while migrating services incrementally with explicit rollback checkpoints between releases. Reviewers would evaluate whether this can replace the wrapper behind a stable interface."
    ],
    "correctAnswers": [
      "Preserve the gateway interface while introducing a new implementation behind the existing service contract for clients. Reviewers would evaluate whether this can replace the wrapper behind a stable interface.",
      "Run contract and regression tests while migrating services incrementally with explicit rollback checkpoints between releases. Reviewers would evaluate whether this can replace the wrapper behind a stable interface."
    ],
    "explanation": "Keeping the gateway contract stable shields client services from replacement details. The team can compare old and new implementations behind the same boundary. This is the mechanism the team needs to replace the wrapper behind a stable interface. A compatibility layer allows selected services to move without changing their calling code immediately. It also leaves a clear place to translate response differences during the transition. The team can verify the behavior with explicit checks before release. Reviewers should document the contract invariants before switching implementations. Tests can then run identical requests against both gateways and compare observable behavior. This keeps the production decision traceable when the system changes. Contract and regression tests detect changes that would violate existing service expectations. Incremental releases limit exposure while evidence is collected from each migrated group. This gives the team a practical way to replace the wrapper behind a stable interface. Explicit checkpoints give operators a known point at which to stop or roll back. This sequence makes parity evidence and operational control part of the migration. It also creates an implementation boundary that can be inspected and tested. A useful rollout record links each cohort to its test results, monitoring window, and fallback decision. That trace supports safe continuation across the remaining services. The team can compare later releases against the same operational standard."
  },
  {
    "number": 46,
    "question": "An agent repeatedly includes old search results and now cites superseded customer status. The revised approach must preserve relevant context and produce measurable behavior across representative inputs. Which TWO actions most directly improve its active context?",
    "options": [
      "Isolate a verification subtask, then return only its validated findings to the main agent. The team would apply the same decision when it must remove stale evidence from active context.",
      "Summarize the complete transcript periodically while retaining stale status in the summary. Reviewers would evaluate whether this can remove stale evidence from active context.",
      "Clear stale tool results after carefully preserving verified current authoritative facts and necessary provenance externally. The selected method would be used to remove stale evidence from active context.",
      "Cache the full transcript and add a recency instruction, while leaving conflicting search results in active context. The selected method would be used to remove stale evidence from active context before release.",
      "Keep all results but label the newest entry as authoritative within prompts. The application would follow this approach to remove stale evidence from active context."
    ],
    "correctAnswers": [
      "Clear stale tool results after carefully preserving verified current authoritative facts and necessary provenance externally. The selected method would be used to remove stale evidence from active context.",
      "Isolate a verification subtask, then return only its validated findings to the main agent. The team would apply the same decision when it must remove stale evidence from active context."
    ],
    "explanation": "Tool result clearing removes content that no longer contributes to the current task. Preserving the current status and required provenance first prevents the cleanup from destroying essential evidence. This gives the team a practical way to remove stale evidence from active context. This approach reduces competing facts inside the context window. It also makes the remaining state easier for both the model and developers to inspect. It also creates an implementation boundary that can be inspected and tested. The application should retain detailed audit history in an external store when policy requires it. Active context and durable records serve different purposes. The team can compare later releases against the same operational standard. A fresh verification subtask can operate without the polluted transcript and focus on current evidence. Returning only validated findings limits context growth in the main agent. In this scenario, that is what allows the team to remove stale evidence from active context. The subtask should receive explicit criteria, trusted sources, and the customer identifier. Its result then becomes a bounded context artifact rather than another raw search dump. This makes the expected behavior easier to measure under production-like conditions. Isolation does not eliminate the need to update authoritative state. The orchestrator must replace or supersede the outdated status after validation. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 47,
    "question": "A tool can repeatedly return the same recoverable error, and the loop below has no operational bound. The team needs clear boundaries for delegation, retries, approvals, and termination. Which change best prevents an unbounded agent run? while True: response = client.messages.create(**request) if response.stop_reason == \"end_turn\": return response request = handle_requested_tools(response, request) Treat every tool_use response as final success, even when the requested operation has not executed yet. The application would follow this approach to bound retries and escalate repeated failures. Remove the end_turn check so every response continues through another tool cycle until process is externally terminated. This would become the basis used to bound retries and escalate repeated failures before release.",
    "options": [
      "response = client.messages.create(**request)",
      "if response.stop_reason == \"end_turn\":",
      "request = handle_requested_tools(response, request)",
      "Treat every tool_use response as final success, even when the requested operation has not executed yet. The application would follow this approach to bound retries and escalate repeated failures.",
      "Remove the end_turn check so every response continues through another tool cycle until process is externally terminated. This would become the basis used to bound retries and escalate repeated failures before release.",
      "Add a maximum turn or tool-call budget and return a controlled terminal error when exceeded. This would become the basis used to bound retries and escalate repeated failures.",
      "Reset conversation after each call so the model cannot observe earlier failures or results. The implementation would treat this as the rule for how to bound retries and escalate repeated failures."
    ],
    "correctAnswers": [
      "Add a maximum turn or tool-call budget and return a controlled terminal error when exceeded. This would become the basis used to bound retries and escalate repeated failures."
    ],
    "explanation": "A turn or tool-call budget places an explicit upper bound on repeated agent cycles. When the repeated tool error consumes that budget, the application can stop predictably. That directly supports the need to bound retries and escalate repeated failures. Returning a controlled terminal error preserves the reason for termination. Callers can distinguish a limit from successful end_turn completion and choose recovery deliberately. It gives reviewers concrete evidence instead of relying on an informal assumption. The bound should match the task's expected complexity and operational budget. Monitoring limit hits can reveal a broken tool, unclear prompt, or unsuitable recovery policy. This also reduces ambiguity when the team investigates a regression."
  },
  {
    "number": 48,
    "question": "A nightly job analyzes thousands of documents against the same long policy and does not need immediate responses. The selected approach must balance quality, cost, latency, and predictable response handling. Which TWO changes best target cost? Stream every response to a browser even though no consumer needs incremental output. The design would use this reasoning to combine prompt caching with asynchronous batch processing.",
    "options": [
      "Stream every response to a browser even though no consumer needs incremental output. The design would use this reasoning to combine prompt caching with asynchronous batch processing.",
      "Send each document synchronously while reordering the policy prefix, which prevents cache reuse across repeated requests. This would become the basis used to combine prompt caching with asynchronous batch processing.",
      "Increase every output limit to the maximum before measuring actual response needs. The selected method would be used to combine prompt caching with asynchronous batch processing.",
      "Submit requests through Message Batches because the workload accepts asynchronous results. The team would apply the same decision when it must combine prompt caching with asynchronous batch processing.",
      "Keep the stable policy prefix identical and enable prompt caching at an appropriate breakpoint. The selected method would be used to combine prompt caching with asynchronous batch processing."
    ],
    "correctAnswers": [
      "Keep the stable policy prefix identical and enable prompt caching at an appropriate breakpoint. The selected method would be used to combine prompt caching with asynchronous batch processing.",
      "Submit requests through Message Batches because the workload accepts asynchronous results. The team would apply the same decision when it must combine prompt caching with asynchronous batch processing."
    ],
    "explanation": "Prompt caching can reduce the cost and latency of repeatedly processing a stable prompt prefix. A shared policy is a natural candidate when its content and ordering remain consistent. In this scenario, that is what allows the team to combine prompt caching with asynchronous batch processing. The breakpoint should cover stable material before document-specific content changes. Cache diagnostics and usage fields can confirm whether the intended prefix is reused. This makes the expected behavior easier to measure under production-like conditions. Caching does not remove those tokens from the context window. The application must still budget the full prompt and output size. That makes the design easier to audit and maintain over time. Message Batches are designed for large groups of requests that can complete asynchronously. That matches a nightly job without interactive latency requirements. This is the mechanism the team needs to combine prompt caching with asynchronous batch processing. Batch processing can be combined with prompt caching for additional savings. Cache hits in concurrently processed batches are best effort, so the team should measure actual results. The team can verify the behavior with explicit checks before release. Batch result handling must preserve each request's custom identifier and terminal status. The job should reconcile successes, errors, and expired entries before publishing output. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 49,
    "question": "A claims-processing service always performs four known steps: extract fields, validate a schema, compare policy rules, and route exceptions for human approval. Only the policy interpretation step benefits from Claude, and the team requires predictable execution paths, bounded cost, and auditable checkpoints. Which architecture best fits these requirements?",
    "options": [
      "Run several independent agents that vote on every step, including deterministic schema validation. The selected method would be used to separate deterministic controls from bounded model reasoning.",
      "Put all four steps into one prompt and rely on Claude to follow the requested order. The design would use this reasoning to separate deterministic controls from bounded model reasoning.",
      "Give an autonomous agent every service tool and let it decide the sequence, completion criteria, and approval timing for each claim. Reviewers would evaluate whether this can separate deterministic controls from bounded model reasoning.",
      "Implement a predefined workflow, use Claude only for bounded policy interpretation, and preserve programmatic validation plus human approval. Reviewers would evaluate whether this can separate deterministic controls from bounded model reasoning."
    ],
    "correctAnswers": [
      "Implement a predefined workflow, use Claude only for bounded policy interpretation, and preserve programmatic validation plus human approval. Reviewers would evaluate whether this can separate deterministic controls from bounded model reasoning."
    ],
    "explanation": "The application has a known sequence, so a predefined workflow supplies the required predictability and auditability. Claude is limited to the interpretation step that actually needs model judgment. The important point is that the design can separate deterministic controls from bounded model reasoning. Schema validation remains programmatic rather than probabilistic. The human checkpoint stays at the consequential exception decision, where oversight has clear value. The implementation can record the result and compare it with the acceptance criteria. This design bounds cost and execution while retaining Claude's useful flexibility. It also prevents autonomous choices from replacing fixed controls that the team already understands. The same evidence can support monitoring, rollback, and later revisions. via - https://www.anthropic.com/engineering/building-effective-agents"
  },
  {
    "number": 50,
    "question": "An MCP server exposes the sales tool definition below. The backend requires region, but calls can omit it. Which schema change most directly fixes the MCP definition? { \"name\": \"get_sales\", \"description\": \"Returns sales totals.\", \"inputSchema\": { \"type\": \"object\", \"properties\": { \"region\": {\"type\": \"string\"} } } }",
    "options": [
      "Add a required array containing region while keeping the property definition in inputSchema. The design would use this reasoning to require every backend field in the tool schema.",
      "Add an input example containing region and remove the properties definition from inputSchema entirely instead. The team would then require every backend field in the tool schema based on that assumption.",
      "Rename region to location without changing the schema's required fields. The application would follow this approach to require every backend field in the tool schema.",
      "Expand the description to ask Claude to include region, but leave the JSON Schema structure itself otherwise unchanged. The selected method would be used to require every backend field in the tool schema."
    ],
    "correctAnswers": [
      "Add a required array containing region while keeping the property definition in inputSchema. The design would use this reasoning to require every backend field in the tool schema."
    ],
    "explanation": "JSON Schema uses a required array on an object schema to identify properties that must be present. Adding region to that array makes the MCP tool contract match the backend requirement. The option therefore enables the implementation to require every backend field in the tool schema. The existing properties entry should remain because it defines region as a string. Required membership and property type describe different parts of the same input contract. The resulting control can be exercised repeatedly during regression testing. A precise inputSchema helps a client construct valid tool input and allows schema-aware validation to catch omissions. The server should still validate arguments before executing the backend call. Future changes can be reviewed without losing the original reasoning."
  },
  {
    "number": 51,
    "question": "A backend service must invoke Claude programmatically, pass structured tool definitions, and process responses without a human interface. The implementation must preserve the required data and execution boundaries under production conditions. Which surface best fits? Require an employee to paste each backend request into claude.ai and copy responses manually back. Reviewers would evaluate whether this can send Messages and handle tools programmatically.",
    "options": [
      "Require an employee to paste each backend request into claude.ai and copy responses manually back. Reviewers would evaluate whether this can send Messages and handle tools programmatically.",
      "Use an official Claude SDK in the backend to submit Messages requests, tools, and response handling. The design would use this reasoning to send Messages and handle tools programmatically.",
      "Run Claude Desktop on the server and automate pointer clicks for every production request. This would become the basis used to send Messages and handle tools programmatically.",
      "Write pending requests into CLAUDE.md files and wait for future Claude Code sessions to discover and execute them. Reviewers would evaluate whether this can send Messages and handle tools programmatically."
    ],
    "correctAnswers": [
      "Use an official Claude SDK in the backend to submit Messages requests, tools, and response handling. The design would use this reasoning to send Messages and handle tools programmatically."
    ],
    "explanation": "An official SDK gives backend code a supported interface for Messages requests and typed response handling. Tool definitions and returned content blocks can be processed without a human-operated chat surface. In this scenario, that is what allows the team to send Messages and handle tools programmatically. The service can apply its own authentication, timeouts, retries, and observability around the API call. That matches the requirement for unattended programmatic integration. This makes the expected behavior easier to measure under production-like conditions. Integration tests should submit a normal message and a tool-enabled message through the same backend boundary. Assertions can verify request construction, response parsing, and tool dispatch behavior. That makes the design easier to audit and maintain over time."
  },
  {
    "number": 52,
    "question": "Claude returns schema-conforming JSON for a production configuration update. Before applying the change, what should the application do next? Any revision must be evaluated against the same quality and latency criteria before release. Apply the configuration immediately because strict schema conformance guarantees every target fact and instruction is correct. The design would use this reasoning to validate authorization and business rules before execution before release. Skip semantic validation whenever all required property names appear in the expected order. The application would follow this approach to validate authorization and business rules before execution.",
    "options": [
      "Apply the configuration immediately because strict schema conformance guarantees every target fact and instruction is correct. The design would use this reasoning to validate authorization and business rules before execution before release.",
      "Skip semantic validation whenever all required property names appear in the expected order. The application would follow this approach to validate authorization and business rules before execution.",
      "Check completion status, validate rules and facts, then authorize the downstream action. This would become the basis used to validate authorization and business rules before execution.",
      "Convert the JSON into prose and trust whichever explanation sounds most confident to an operator. The team would then validate authorization and business rules before execution based on that assumption."
    ],
    "correctAnswers": [
      "Check completion status, validate rules and facts, then authorize the downstream action. This would become the basis used to validate authorization and business rules before execution."
    ],
    "explanation": "The application should first confirm that generation ended in an acceptable state rather than truncation or refusal. It should then validate the returned values against trusted deployment records and change policy. This is the mechanism the team needs to validate authorization and business rules before execution. Domain validation can enforce environment identifiers, allowed settings, dependencies, and required evidence. These checks are outside the expressive guarantee of output schema conformance. The team can verify the behavior with explicit checks before release. Only after those controls pass should the service authorize the downstream configuration change. Audit logging can preserve the model output, validation result, and action decision. This keeps the production decision traceable when the system changes."
  },
  {
    "number": 53,
    "question": "During an invalid-request incident, this wrapper generates a burst of repeated calls. Reviewers want to avoid an optimization that hides the actual cause of the production behavior. Which correction best addresses the defect? def send_with_retry(client, params): while True: try: return client.messages.create(**params) except Exception: continue Remove handling so the process exits whenever a request encounters an error. The selected method would be used to avoid retrying requests that cannot succeed unchanged.",
    "options": [
      "Remove handling so the process exits whenever a request encounters an error. The selected method would be used to avoid retrying requests that cannot succeed unchanged.",
      "Catch every exception, but add a debug log before starting the next immediate attempt. The selected method would be used to avoid retrying requests that cannot succeed unchanged.",
      "Retry invalid requests using progressively larger payloads because added content will eventually make every malformed request valid. The selected method would be used to avoid retrying requests that cannot succeed unchanged.",
      "Classify failures, retry only transient cases with bounded backoff, and surface nonretryable errors. The design would use this reasoning to avoid retrying requests that cannot succeed unchanged."
    ],
    "correctAnswers": [
      "Classify failures, retry only transient cases with bounded backoff, and surface nonretryable errors. The design would use this reasoning to avoid retrying requests that cannot succeed unchanged."
    ],
    "explanation": "The loop currently treats configuration errors, authentication failures, and transient service conditions alike. Retrying nonretryable errors cannot correct the request and amplifies traffic. The important point is that the design can avoid retrying requests that cannot succeed unchanged. A sound wrapper classifies failures and uses a finite retry policy for conditions that can plausibly recover. Backoff and jitter help avoid synchronized retry bursts. The implementation can record the result and compare it with the acceptance criteria. The official SDK already retries selected connection, rate-limit, and server errors by default. Custom logic should complement that behavior and surface terminal failures to the caller. The same evidence can support monitoring, rollback, and later revisions."
  }
]